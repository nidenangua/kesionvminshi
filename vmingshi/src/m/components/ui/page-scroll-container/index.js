import PageScrollContainer from './PageScrollContainer';

/* install component */
PageScrollContainer.install = function(Vue) {
    Vue.component(PageScrollContainer.name, PageScrollContainer);
};

export default PageScrollContainer;